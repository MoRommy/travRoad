<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ro.scoalainformala.travroad">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Travroad">

        <activity android:name=".scenes.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".scenes.add_trip.AddTripActivity">

        </activity>

        <activity android:name=".scenes.splash.SplashActivity"
                  android:theme="@style/SplashScreenTheme">

        </activity>

        <activity android:name=".scenes.trips.activity.TripsActivity">

        </activity>

    </application>

</manifest>